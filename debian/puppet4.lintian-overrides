# Man pages are automatically generated, not much to do here
puppet4 binary: manpage-has-bad-whatis-entry

# puppet manifests many use puppet as interpreter
puppet4 binary: example-unusual-interpreter * #!puppet

# Conditional update-rc.d invocations to handle the puppet-agent deprecation
puppet4: duplicate-updaterc.d-calls-in-postinst puppet
